#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#define N_VALIES 5
/*int main() {
	float values[N_VALIES];
	float* vp;
	for (vp = &values[N_VALIES]; vp > &values[0];) {
		*--vp = 0;
	}
	//for (vp = &values[N_VALIES-1]; vp >= &values[0];vp--) {
	//	*vp = 0;
	//}
	//允许指向数组元素的指针与指向数组最后一个元素后面的那个内存位置的指针比较
	//但不允许与指向第一个元素之前的那个内存位置的指针进行比较
	return 0;
}*/

